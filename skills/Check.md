---
name: check
description: 通用技术文档与图片审查标准；用于原理图、流程图、组网图、界面截图的一致性、正确性和敏感信息检查
kind: check
---

# 通用检查执行协议

只执行用户要求且本 Skill 明确定义的检查项，不补充或猜测其他审查标准。

输出格式（严格遵守）：

- 通过：第一行输出 `PASS`，随后可附一两句简要说明；
- 不通过：第一行输出 `FAIL`，随后每行一条具体问题，指明图中位置或文字证据，并说明与文档何处不一致或问题原因；
- 图片不属于本检查项适用对象：第一行输出 `NA`，随后一句话说明原因。

## execution

主 Agent 负责编排（判断素材规模、派遣子 Agent、汇总结论），文件子 Agent 负责具体执行
（读取文档、调用通用 `image_reasoning` 工具、落盘报告）；Core 不内置检查项、
适用性判断或报告格式，下面的检查项只能由子 Agent 按本 Skill 执行。

1. 根据用户原始需求选择检查项：用户明确点名时只执行点名项；未点名时执行适用于当前图片的全部检查项。不得补充本 Skill 以外的检查标准；
2. 只有检查项需要图文一致性时才读取对应文档。文档按规模分级处理（`list_dir`/`find_files` 会返回文件大小；约 20KB 以上视为大文档，`read_document` 对它们只返回开头预览）：
   - 不涉及文档，或全部为小文档：一次 `delegate_task` 派一个子 Agent 读取全部所需文档并执行检查；
   - 涉及一个或多个大文档：先逐份 `delegate_task` 各派一个子 Agent 提炼——每个子 Agent 只负责一份大文档，可用 `force_read=true` 全读（其上下文汇报后即销毁），把与图片和检查项相关的内容提炼为摘录文件（如 `check_results/<任务>/extracts/<文档名>.md`）——再把摘录文件与小文档一起交给执行检查的子 Agent。不要把多份大文档原文塞给同一个子 Agent，它的上下文也顶不住；
   - 只需核对局部内容时，让子 Agent 用 `grep_files` 在大文档中定位相关段落，不通读全文；
3. 每个“图片 × 检查项”由子 Agent 单独调用一次 `image_reasoning`。prompt 必须包含该检查项的完整说明、必要文档内容以及本 Skill 的 `PASS/FAIL/NA` 输出协议；图片通过 `image_paths` 传入；
4. 不要预先调用视觉模型做一轮通用图片描述，也不要根据文件名直接判断通过、不通过或不适用；适用性也由该次 `image_reasoning` 按检查项的 `applies_to` 判断；
5. 收到工具结果后，按首行 `PASS`、`FAIL`、`NA` 记录，不改写模型结论；
6. 所有调用完成后，由子 Agent 用 `write_file` 写 CSV。列固定为 `image,check_id,check_name,result,findings`，一行对应一次图像推理；主 Agent 随后向用户汇总通过、不通过、不适用数量和报告路径。

## batch

用户要求批量检查某个目录、目录下全部图片或一份素材清单时，拆成两个阶段：

规划阶段只负责素材发现和配对：

1. 子 Agent 用 `list_dir` 扫描用户指定目录及必要子目录，只处理受支持的图片和文本型文档；
2. 每张图片形成一个 case，优先配对同目录下同名文档；
3. 同目录内未与任何图片同名的文档可作为该目录的共享依据；
4. 不跨子目录猜测配对，存在歧义时写入 `warnings`；
5. 只有目录和文件名不足以判断配对时才调用 `read_document`，不要为了规划通读长文档；
6. 用 `write_file` 写出 `batch_plan.json`，顶层包含 `version`、`directory`、`cases`、`warnings`，每个 case 至少包含 `id`、`image`、`documents`；
7. 文件写入成功后规划子 Agent 立即结束，不调用图片读取、图片质检或检查模型。

执行阶段由上层编排器读取计划，再逐 case 发起彼此独立的图片质检；每次只传该 case 的图片与文档。单个 case 完成后立即保留结果，失败不得丢弃已完成案例，最后再汇总。

计划中的路径必须是当前 Session 内的相对路径，不得写入服务器绝对路径。

## check: schematic_consistency | 原理图与原理描述一致性

applies_to: 原理图

你是技术文档质量审核员。附件图片应为原理图。核对它与下方原理描述是否一致。

<document>
{document}
</document>

检查：描述中的组件是否完整出现且无臆造；组件连接、信号或数据流向及箭头方向是否一致；名称、术语和缩写是否一致；图示因果、流程和层级是否自洽。描述未涉及的图面细节和样式不作为失败理由；描述有歧义时按合理解释通过。

## check: flowchart_correctness | 流程图正确性

applies_to: 流程图

你是技术文档质量审核员。附件图片应为流程图。只检查流程图自身逻辑，不核对它与文档步骤的一致性。

<document>
{document}
</document>

检查：分支闭合且都有去向，无死循环或不可达节点；步骤顺序符合业务逻辑；判断节点出边有标签并最终回主流程或结束；有明确开始和结束。颜色、字体和间距不作为失败理由。

## check: flowchart_steps | 流程图与操作步骤一致性

applies_to: 流程图

你是技术文档质量审核员。附件图片应为流程图。核对它与下方操作步骤是否一致。

<document>
{document}
</document>

检查：文档每一步均在图中出现且无遗漏或臆造；步骤先后和依赖关系一致；判断条件、分支标签和走向一致；节点文字与步骤用语语义一致。文档有歧义时按合理解释通过；样式不作为失败理由。

## check: network_correctness | 组网图正确性

applies_to: 组网, 拓扑

你是网络技术文档审核员。附件图片应为组网或拓扑图。只检查拓扑自身正确性，不核对它与组网描述的一致性。

<document>
{document}
</document>

检查：IP、网段、VLAN、接口标识无冲突或明显错误；网关位置、层级和环路风险合理；设备均有连接路径且无孤立设备；设备角色与位置合理。样式和布局美观度不作为失败理由。

## check: network_consistency | 组网图与组网描述一致性

applies_to: 组网, 拓扑

你是网络技术文档审核员。附件图片应为组网或拓扑图。核对它与下方组网描述是否一致。

<document>
{document}
</document>

检查：设备数量、型号和角色一致；连接关系和链路类型一致且无缺漏；IP 网段、VLAN 和接口标识符合地址规划；设备命名和术语一致。描述未提及的图面细节不作为失败理由。

## check: ui_correctness | 界面截图正确性

applies_to: 界面截图, 界面

你是技术文档质量审核员。附件图片应为界面截图。检查截图是否正确并与下方文档描述的实际操作界面一致。

<document>
{document}
</document>

检查：页面、窗口或功能模块与文档一致；当前页签、表单状态和提示信息符合对应操作节点；没有报错、空白或乱码等明显异常。文档未涉及该截图时只做基本合理性检查；清晰度和美观度不作为失败理由。

## check: ui_terminology | 界面词与实际界面一致性

applies_to: 界面截图, 界面

你是技术文档质量审核员。附件图片应为界面截图。核对文档界面用词与截图中的实际文字。

<document>
{document}
</document>

检查：文档引用的按钮、菜单、页签和字段名在截图中存在且文字一致；逐条指出凭空引用的界面元素；术语与截图用语一致。文档未引用具体界面词时按通过处理并说明。

## check: ui_sensitive | 图片敏感信息检查

applies_to: *

你是文档安全审核员。检查附件图片是否包含不应出现在对外文档中的敏感信息。

<document>
{document}
</document>

发现以下任一项即失败并指出位置和内容：公网 IP；真实公网域名或对外服务地址；用户名、账号、工号、手机号或邮箱；密码、密钥、token 或证书内容。192.168.x.x、10.x.x.x、172.16-31.x.x、127.x.x.x 等内网地址，VLAN、普通设备名，以及 example.com、user@example、xxx 等示例或脱敏占位符不算敏感。本项不报告图文一致性问题。

## check: ui_steps | 界面截图与操作步骤一致性

applies_to: 界面截图, 界面

你是技术文档质量审核员。附件图片应为界面截图。核对截图与下方操作步骤的一致性。

<document>
{document}
</document>

检查：截图状态与文档引用它的操作节点相符；多张截图的顺序与操作步骤一致且衔接合理；文档声称的操作结果能在截图中得到印证。文档未明确关联截图与步骤时按合理解释通过。
