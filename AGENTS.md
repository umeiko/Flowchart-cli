# AGENTS.md

## 项目工作方式：快速迭代优先

本仓库用于快速验证 Flowchart AI 的产品想法。默认交付目标是能回答当前问题的最小可运行实验、Demo 或 POC，而不是生产级交付。除非用户明确要求，不要把“实现”“完成”“跑起来”“按计划执行”解释为生产化、全面硬化或完整质量门禁。

在已授权范围内，优先完成最短真实 happy path，尽早产出用户可观察、可试用的结果。与当前结果无关的审查、重构、抽象、兼容性处理和工程化工作不得阻塞主路径。

系统、平台和用户指令始终优先于本文件。本文件限制的是过度检查，不取消避免数据丢失、密钥泄露、未经授权外部影响等必要安全边界。

## 开始修改前

- 先运行 `git status --short --branch`，了解并保护用户已有改动。
- 只检查本轮目标相关的文件。不要把普通任务扩成全仓梳理。
- 工作区可能长期保持 dirty；不得覆盖、回退、格式化或顺手修改无关内容。
- 不读取、打印、复制或提交 `.env` 的值。只能在确有必要时检查所需变量是否存在。

## 默认实施深度

- 只实现当前需求所需的最小闭环，保留用户要求的阶段、连接和可观察行为。
- 优先直接代码和局部修改。没有重复需求证据时，不提前建设通用框架、插件层、迁移层或复杂配置系统。
- 不主动追加生产硬化、性能调优、广泛边界处理、跨平台兼容、依赖升级、发布门禁或大规模重构。
- 遇到旁支问题时，只在它真实阻塞当前 happy path 时处理；否则在交付说明中简短指出即可。
- 不因“可能存在风险”而自动扩大范围。先用当前证据判断；推测性风险不是新增审计工作的授权。

## 安全审查默认不阻塞

除非用户明确要求、验收契约明确要求、缺少检查就无法判断当前路径是否可用，或更高层规则强制要求，否则不要主动执行：

- 通用安全审计、威胁建模、权限审计或依赖审计；
- 渗透测试、模糊测试、全仓静态扫描；
- 完整回归、测试矩阵、覆盖率工程或额外审阅轮次；
- 与当前修改无关的文档、历史实现和配置清查；
- 为“更稳妥”而进行的生产级 hardening。

任何次要检查只有在真正独立、不会产生重叠写入或稀缺资源争用、不会增加实质协调成本、且不会拖慢主结果时才可并行。默认交付不得等待这些非阻塞检查。

## 最小直接验证

代码修改后，默认只运行一到两次最直接覆盖本轮变化的验证。一个聚焦测试或一次简单 smoke run 通过，通常就足够交付。

- 优先运行单个相关测试文件、单个测试用例或最小命令路径。
- 不要在聚焦验证通过后继续扩大为 full lint、全量 `pytest`、全量回归或工程级验收。
- 验证失败时，先针对该失败做最小诊断和最小修复，然后重跑同一路径。只有现有证据不足以定位时，才逐步增加最少必要检查。
- 用户说“简单测试即可”时，使用少量真实数据或真实操作，并加入机械可核对的结果检查；不要升级为长期实验或生产级验收。
- 纯文档改动只检查本轮涉及的相对链接、文件路径、提交号和“已实现”声明；不要因此运行代码全回归。

交付时必须如实说明：运行了什么、使用什么输入和环境、观察到什么、能证明什么，以及不能证明什么。局部测试不能描述成完整链路、生产级或长期稳定验证。

## 整体流程的最小端到端例外

当用户要验证整体设计、Agent 流水线、生成—校验—渲染—反馈链路或其他工作流是否可行时，各必要阶段及其连接本身就是交付物。此时最小验证应是一次最小真实端到端执行，而不是若干互不相连的组件 Demo。

- 使用同一份冻结输入或明确冻结的小型场景集。
- 下游必须消费真实上游输出。
- 用同一 run、trace、会话标识或等价证据说明结果确实连通。
- 互斥分支可由同一小型场景集中的不同样本覆盖。
- 端到端尝试失败可以得出“失败”或“证据不足”；没有实际尝试时，不得宣称整体方案完成或可行。

“Demo”“POC”“只要跑起来”“跳过小问题”只降低工程深度，不自动删除用户明确要求的功能阶段或连接。只有用户明确缩小范围时才能删除。

## 具体 Bug 修复的窄例外

只有已经真实复现、随后完成修复的具体 Bug，才自动增加一次独立新 Codex task/session 复验。普通功能开发、推测性风险、文档修正和未复现缺陷不触发该要求。

- 当前任务先完成最小直接验证。
- 新任务只接收中性 bug report、原始复现步骤、目标 revision 或路径、必要约束和期望行为；不要提供当前任务的诊断、修改位置或结论。
- 独立复验只执行原始聚焦路径，不扩大成全量回归或安全审计。
- 若新任务无法执行真实路径、无法区分成功失败或仍能复现，只能报告“未独立验证”或“仍失败”。

## 本仓库的成本与状态边界

- 会调用模型、启动完整 Agent 循环、批量生成图、批量视觉校验或重建大型产物的命令都可能耗时并写入状态。除非用户明确要求实际运行，先用 `--help`、已有 preflight、单个小 fixture 或不调用模型的聚焦测试确认输入。
- 输入确认不等于授权启动完整昂贵流程。不要因为命令可运行就自动执行批量任务。
- 若需要真实 smoke run，优先使用一个小输入，例如单个 `test_datas/` fixture，并把输出写入本轮专用目录，避免污染用户现有 `output/`、缓存、会话或实验状态。
- Mermaid、draw.io、浏览器和视觉模型链路只验证本轮实际修改触及的部分。不要默认同时验证所有引擎、所有样式和所有平台。
- 不要把旧的 `output/`、日志、缓存、截图或报告当作本轮成功证据。
- 不运行破坏性重置，不清空用户输出、缓存、会话或实验目录。

## 交付说明

最终回复保持简洁，先给结果，再说明最小验证和已知边界。不要用检查数量、覆盖率或报告篇幅代替对本轮可观察结果的说明，也不要把 POC 包装成生产就绪实现。
