"""验证 prompt：FlowchartAgent 生成-验证循环的检视端（三档强度）。"""

_VERIFY_OUTPUT_FORMAT = """输出格式（严格遵守）：
- 全部通过：只输出一行 PASS
- 存在问题：第一行输出 FAIL，随后每行一条具体问题（指明节点名和错误）。"""

# 基础图形检视：只查排版与结构，不逐字核对内容。
# 适用于视觉模型文字识别能力弱的场景——避免模型读错字导致永远无法通过。
VERIFY_LAYOUT_PROMPT = """你是图表渲染质量审核员。附件图片是渲染出的图表（流程图、架构图等），本次只检查图形排版与结构，不要核对文字内容。

检查维度（仅图形层面）：
1. 文字排版：节点文字是否被截断、溢出节点框、相互重叠；
2. 连线结构：连线是否混乱缠绕、穿过节点、重叠到难以辨认，箭头是否连在节点上；
3. 节点遮挡：节点框之间是否相互遮挡、重叠；
4. 分支标签：如有判断节点，其分支连线上是否带有标签文字（无需确认标签写得对不对，只看有没有、是否清晰可读）。

宽容规则（重要，防止误判）：
- 不要逐字辨认或核对节点文字的内容，文字语义正确性不在本次检查范围内；
- 颜色、背景、字体、间距、对齐等样式与美观问题一律不作为 FAIL 理由。

""" + _VERIFY_OUTPUT_FORMAT

# 代码检视：完全没有视觉模型时的兜底——文本模型直接审查 Mermaid 源码。
# 查内容与逻辑，但查不了排版/遮挡（那是渲染层问题，只能如实提示用户）。
VERIFY_CODE_PROMPT = """你是一个严格的图表审核员。下面的 Mermaid 代码是根据文档生成的图表源码（流程图、架构图等；本次没有渲染图可看），请审查代码是否忠实表达了文档，且语法结构合理。

<document>
{document}
</document>

<mermaid_code>
{code}
</mermaid_code>

检查维度：
1. 节点完整性：文档中的每个步骤/组件是否都在代码中出现，有无遗漏或臆造；
2. 连接与方向：节点间的先后/依赖/层级关系、箭头方向是否正确；
3. 分支逻辑（仅流程类文档）：判断分支是否与文档一致，判断节点的每个出边是否都有标签；
   架构/分层类文档没有判断分支，改为检查分层、分组（subgraph）是否与文档一致；
4. 文字一致性：节点文字是否与文档语义一致；
5. 语法结构：节点 id 只用英文/数字/下划线、未使用 Mermaid 保留字（end/call/wait/click/
   class/style/default 等）作 id、含特殊字符的文字是否被英文双引号包裹、
   每条语句独占一行。

宽容规则（重要，防止误判）：
- 流程类文档补全的常规起止节点（开始/结束）不算臆造，只要不与文档内容冲突；
- 架构/分层类图表默认各层之间不画可见连线/箭头（分层条带画法），
  不得以"缺少连线/箭头"为由判不通过；此类图表重点核对层级顺序与分组包含关系；
- 样式（颜色、字体、间距等）不作为 FAIL 理由；
- 文档表述有歧义时，按合理解释通过，不要苛求。

""" + _VERIFY_OUTPUT_FORMAT

# 完整检视 = 基础图形检视 + 内容与逻辑核对（默认）。
VERIFY_PROMPT = """你是一个严格的图表审核员。附件图片是根据下方文档渲染出的图表（流程图、架构图等），请检查图片是否忠实表达了文档。

<document>
{document}
</document>

检查维度：
1. 节点完整性：文档中的每个步骤/组件是否都在图中出现，有无遗漏或臆造；
2. 连接与方向：节点间的先后/依赖/层级关系、箭头方向是否正确；
3. 分支逻辑（仅流程类文档）：文档中的判断分支是否与图一致，分支标签是否正确；
   架构/分层类文档没有判断分支，改为检查分层、分组（subgraph）是否与文档一致；
4. 文字一致性：节点文字是否与文档语义一致；
5. 排版结构：文字是否被截断/溢出/重叠，节点框是否相互遮挡，连线是否混乱缠绕或穿过节点。

样式宽容规则（重要，防止误判）：
- 除非文档明确指定了颜色、背景、字体等视觉样式，否则样式问题一律不作为 FAIL 理由；
- 文档指定了画布背景色时，只检查画布背景是否符合，节点配色仍不作为 FAIL 理由；
- 流程类文档补全的常规起止节点（开始/结束）不算臆造，只要不与文档内容冲突；
- 架构/分层类图表默认采用"分层条带"画法：**各层之间没有可见连线/箭头是正常设计，
  不得以"缺少连线/箭头/流向"为由 FAIL**；此类图表应核对层级顺序、分组包含关系
  与组件完整性；
- **存在空分组/空层（标题存在但内部没有任何组件）时一律 FAIL**——这通常意味着
  该层的内容被遗漏，对照文档指出缺少的组件；
- 布局美观度（间距、对齐等）不作为 FAIL 理由，可在 FAIL 之外不涉及。

""" + _VERIFY_OUTPUT_FORMAT
