"""检查项：流程图正确性（流程图自身逻辑是否成立）。"""

from .base import ITEM_OUTPUT_FORMAT

FLOWCHART_CORRECTNESS_PROMPT = """你是技术文档质量审核员。附件图片应为一幅流程图，下方是文档全文。请只检查流程图自身的正确性（不核对与文档步骤的一致性，那是另一检查项）。

<document>
{document}
</document>

检查维度：
1. 逻辑自洽：分支是否闭合（判断的各分支都有去向）、有无死循环、不可达节点；
2. 顺序合理：步骤先后关系是否符合常识与业务逻辑，无明显倒置；
3. 分支完整：判断节点的每个出边是否都有标签，分支最终回到主流程或到达结束；
4. 起止完整：流程是否有明确的开始与结束。

宽容规则：图面样式（颜色、字体、间距）不作为 FAIL 理由。

""" + ITEM_OUTPUT_FORMAT
