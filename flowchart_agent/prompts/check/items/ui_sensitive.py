"""检查项：界面截图敏感信息检查（公网 IP、账户信息等）。适用于任何图片。"""

from .base import ITEM_OUTPUT_FORMAT

UI_SENSITIVE_PROMPT = """你是文档安全审核员。附件图片来自一份技术文档。请检查图片中是否包含不应出现在对外文档中的敏感信息。

<document>
{document}
</document>

检查维度（发现任何一项即 FAIL，并指明具体位置与内容）：
1. 公网 IP 地址（注意区分：192.168.x.x、10.x.x.x、172.16-31.x.x、127.x.x.x 为内网地址，
   技术文档中出现内网地址是正常内容，不算敏感）；
2. 真实公网域名、真实对外服务地址；
3. 账户信息：用户名、账号、工号、手机号、邮箱；
4. 凭据：密码、密钥、token、证书内容。

宽容规则：
- 内网 IP、VLAN、设备命名（如 SW-Core、FW-01）等内部组网信息是技术文档的正常内容，
  不算敏感信息；
- 文档中的示例占位符（如 example.com、user@example、xxx 脱敏内容）不算敏感信息；
- 本项只查敏感信息泄露，图与文档的一致性问题不属于本项，不要报告。

""" + ITEM_OUTPUT_FORMAT
