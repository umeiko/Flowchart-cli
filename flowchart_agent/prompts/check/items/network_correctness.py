"""检查项：组网图正确性（拓扑自身是否成立）。"""

from .base import ITEM_OUTPUT_FORMAT

NETWORK_CORRECTNESS_PROMPT = """你是网络技术文档审核员。附件图片应为一幅组网（拓扑）图，下方是文档全文。请只检查组网图自身的正确性（不核对与组网描述的一致性，那是另一检查项）。

<document>
{document}
</document>

检查维度：
1. 规划自洽：IP 地址/网段/VLAN/接口标识有无冲突（如两个设备同 IP）、有无明显错误；
2. 拓扑合理：拓扑结构是否符合常见组网规范（网关位置、层级结构、有无环路风险）；
3. 链路完整：设备是否都存在连接路径，有无孤立设备；
4. 角色合理：设备角色与所处位置是否合理（如服务器不应直接暴露公网）。

宽容规则：图面样式与布局美观度不作为 FAIL 理由。

""" + ITEM_OUTPUT_FORMAT
